<%= form_for(@warrior) do |f| %>
  <% if @warrior.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@warrior.errors.count, "error") %> prohibited this warrior from being saved:</h2>

      <ul>
      <% @warrior.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_area :code %>
  </div>
  <div class="field">
    <%= f.label :level %><br />
    <%= f.number_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
